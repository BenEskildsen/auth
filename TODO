
  - break out user paths into their own express app module
  - same for dashboard
  - turn into npm package(?)

  - later:
    - allow log out (deleting token and/or cookie)
    - allow changing password (and username)
    - token expiration and refreshing
